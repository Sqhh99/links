# 工作记录：将项目升级到 C++20

- **日期：** 2026-09-05
- **分支：** `chore/upgrade-cpp20`（基于 `main` 的 `c61702f`）
- **关联文档：** [PR 记录](../pull-requests/2026-09-05-upgrade-cpp20.md)

## 一、用户的请求

> 将项目升级到c++20，改动项目文件，不需要执行构建，我来构建

将项目要求的 C++ 语言标准从 C++17 升级为 C++20，并同步相关项目文档；构建由用户执行。

## 二、制定的计划

1. 检查构建配置和文档中的 C++17 声明。
2. 将 CMake 构建标准更新为 C++20，并同步 README 和开发指南。
3. 通过静态搜索检查是否还存在有效的 C++17 配置或说明。
4. 按用户要求不执行构建和测试。

## 三、具体改了哪些文件

| 文件 | 改动 | 对应问题 |
| --- | --- | --- |
| `CMakeLists.txt` | 将 `CMAKE_CXX_STANDARD` 从 17 改为 20；采用 Qt 6.8 标准项目配置并移除重复的手工设置 | 使用标准 C++20 协程头，并按 Qt 官方方式启用包括 QTP0004 在内的现代策略 |
| `README.md` | 将编译器要求更新为 C++20 | 保持使用说明与实际构建配置一致 |
| `AGENTS.md` | 将编码标准说明更新为 C++20 | 保持仓库开发指南与实际构建配置一致 |
| `docs/work-logs/2026-09-05-upgrade-cpp20.md` | 记录本次请求、改动和验证边界 | 满足仓库强制工作记录要求 |
| `docs/pull-requests/2026-09-05-upgrade-cpp20.md` | 记录 PR 的关联问题、改动、验证和检查项 | 满足仓库创建 PR 前的强制记录要求 |

## 四、验证情况

已通过静态搜索检查项目自身的构建配置和文档，确认原有 C++17 声明均已更新为 C++20，并确认 Qt 标准项目配置位于首次 `find_package(Qt6 ...)` 之后和目标创建之前。

用户反馈在加入 Qt 标准项目配置前，Windows Release 构建已经成功。加入该配置后的构建、测试和运行时验证未执行，这是用户的明确要求；用户将在 Windows 环境中自行构建。

AI 使用披露：本次构建配置、文档同步和工作记录由 Codex 协助完成；仅做了静态检查，尚未经过实际构建验证。

## 五、遗留事项

需要由用户在 Windows 环境重新执行 Release 构建，确认 QTP0004 警告消失，并检查标准项目配置生成的 QML `qmldir` 文件。

## 六、后续请求：创建分支、提交并创建 PR

> 新建分支提交pr

在 `chore/upgrade-cpp20` 分支提交本次变更，并使用关联的 PR 记录作为 PR 正文。保留工作区中与本次升级无关的 `.gitignore` 改动和 `AGENTS.md` 既有未提交内容，不将其纳入本次提交。

## 七、后续请求：采用 Qt 标准项目配置

> 推荐使用这个qt_standard_project_setup，规范更加重要，修改代码，改完告诉我，我来构建

在首次 `find_package(Qt6 ...)` 后调用 `qt_standard_project_setup(REQUIRES 6.8)`，统一启用 Qt 6.8 及以前的现代 CMake 策略。移除由该命令接管的 `GNUInstallDirs`、`CMAKE_AUTOMOC` 和 `CMAKE_AUTOUIC` 手工设置，避免重复配置。按用户要求不执行构建或测试。
